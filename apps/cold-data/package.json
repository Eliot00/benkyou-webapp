{
  "name": "@benkyou/cold-data",
  "type": "module",
  "scripts": {
    "dev": "export SSL_CERT_FILE=/etc/ssl/certs/ca-certificates.crt && wrangler dev",
    "deploy": "wrangler deploy --minify --keep-vars",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "devDependencies": {
    "wrangler": "catalog:"
  }
}
